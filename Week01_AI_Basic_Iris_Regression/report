# 第一周实习报告：AI认知、工具流与基础机器学习

## 一、项目背景
本周是 AI 科研实践线上实习的第一周。本周目标主要是建立对 AI 工具链的认知，熟悉 GitHub 与 Kaggle 平台，并通过两个经典项目——Iris 分类和 California Housing 回归——掌握机器学习的基础流程。我完成了从数据探索、特征可视化、模型训练到评估的完整流程，并对比了不同算法的效果。

## 二、Iris 鸢尾花分类实验分析

### 2.1 数据基本信息与探索
Iris 数据集包含 150 个样本，特征为花萼长度、花萼宽度、花瓣长度和花瓣宽度，标签为 3 种鸢尾花。数据无缺失值，三类样本各 50 个，分布极其均衡。

通过相关性热力图分析发现，花瓣长度与花瓣宽度呈极强的正相关，相关系数高达 0.96，而萼片宽度与其他特征的相关性较弱。在散点图中可以清晰看到，Setosa 类的花瓣特征显著小于另外两类，且与其他类别没有重叠，这说明花瓣特征在分类中起到了决定性作用。

### 2.2 模型训练与性能对比
我将数据集按 7:3 划分为训练集和测试集。由于 KNN 和逻辑回归对量纲敏感，我对数据进行了标准化处理。选用了三个模型进行对比，测试集准确率如下：

| 模型 | 准确率（Accuracy） |
| Logistic Regression | **0.9111** |
| KNN (k=5) | **0.9111** |
| Decision Tree | **0.9333** |


三个模型的混淆矩阵如下：
- **Logistic Regression**：仅有1个 Versicolor 被误判为 Virginica，3个 Virginica 被误判为 Versicolor。
- **KNN (k=5)**：4个 Virginica 被误判为 Versicolor，其余分类完全正确。
- **Decision Tree**：3个 Versicolor 被误判为 Virginica，其余分类完全正确。

总体来看，决策树在本测试集上准确率最高（0.9333），但三个模型表现接近，均具有较好的分类能力。

### 2.3 特征重要性分析
为了回答"哪个特征对分类最有帮助"，我采用了两种方法进行分析：

**方法一：决策树的特征重要性（Gini 重要性）**
| 特征 | 重要性 |
| petal length (cm) | **0.5641** |
| petal width (cm) | 0.4226 |
| sepal length (cm) | 0.0133 |
| sepal width (cm) | 0.0000 |

**方法二：逻辑回归系数绝对值（多分类取平均）**
| 特征 | 系数绝对值 |
| petal width (cm) | **1.5973** |
| petal length (cm) | 1.2106 |
| sepal length (cm) | 0.7182 |
| sepal width (cm) | 0.6364 |

两种方法均指向花瓣特征是最具区分度的特征，贡献了绝大部分的分类信息。决策树认为花瓣长度略胜一筹，而逻辑回归认为花瓣宽度系数更大。这与我之前在散点图中的观察完全吻合——花瓣特征在三种鸢尾花之间存在明显分界。

## 三、California Housing 房价预测实验分析

### 3.1 目标变量与特征相关性
该数据集包含 20,640 个样本，涉及收入、房龄、房间数、经纬度等 8 个特征。首先绘制了目标变量（房价中位数）的分布图，发现数据呈现右偏分布，大部分房价集中在 0.5 至 3.0（即 5万至 30万美元）之间，存在少量极高值。

在相关性热力图中，我发现MedInc与房价的相关性最强约 0.69，说明收入水平是影响房价的最核心因素。此外，经纬度与房价也表现出相关性。值得注意的是，平均房间数与平均卧室数高度相关为0.85，可能存在多重共线性问题。

### 3.2 回归模型构建与指标对比
为了预测房价，我构建了普通线性回归和带 L2 正则化的岭回归。为防止量纲影响，我对所有特征进行了标准化。两个模型在测试集上的具体指标如下：

| 模型 | MSE (均方误差) | RMSE (均方根误差) | MAE (平均绝对误差) | R² (决定系数) |
| Linear Regression | **0.5559** | **0.7456** | **0.5332** | **0.5758** |
| Ridge (alpha=1.0) | **0.5559** | **0.7456** | **0.5332** | **0.5758** |

### 3.3 结果讨论与残差分析
从 R² 来看，两个模型均能解释约 57.6% 的房价波动。Ridge 回归在所有指标上均略微优于普通线性回归，R² 从 0.575788 提升至 0.575816，这说明正则化项的引入有效抑制了特征间的多重共线性，提升了模型的泛化能力，尽管提升幅度有限。

通过观察"真实值 vs 预测值"散点图，我发现模型在房价处于 1.0~3.0 区间时拟合效果较好，但对于房价高于 4.0 的高价区域，预测值普遍低于真实值。残差图也验证了这一点，随着预测值增大，残差呈现出一定的发散趋势。这提示我们，对于高价房这种样本，可能需要引入非线性模型或进行目标变量的对数变换来进一步优化。

## 四、工具使用与问题解决记录

### 4.1 工具链搭建
本周完成了基础环境配置：注册了 GitHub 与 Kaggle 账号，在本地搭建了 Python 3.x 环境，并安装了 scikit-learn、pandas、matplotlib 和 seaborn 库。我采用了 VS Code 作为主要 IDE，并配合终端（PowerShell）运行脚本。

### 4.2 遇到的关键问题及解决
1. **路径报错（FileNotFoundError）**：运行代码时提示无法保存图片至 `results/` 目录。通过查阅资料，我在代码开头加入了 `os.makedirs('results', exist_ok=True)`，成功实现了文件夹的自动创建，确保了项目在不同设备上的可移植性。
2. **中文字体警告（UserWarning: Glyph missing）**：matplotlib 默认字体 Arial 不支持中文显示，导致图表标题中的中文无法正常渲染。虽然不影响图片保存和核心功能，但影响了可视化效果。后续可通过安装中文字体或设置 `plt.rcParams['font.sans-serif'] = ['SimHei']` 来解决。
3. **标准化对模型的影响**：在 Iris 分类中对特征进行标准化处理，确保 KNN 的距离度量不受量纲影响，逻辑回归能够更快收敛。

### 4.3 本周收获
本周通过两个实战项目，我深刻理解了机器学习项目的过程：数据清洗与探索（EDA）→ 可视化分析 → 特征工程（标准化）→ 基线模型建立 → 模型对比与调优 → 结果评估。Iris 项目让我巩固了分类问题的评估指标，而 Housing 项目让我掌握了回归问题的评估体系以及正则化防止过拟合的实际应用。
