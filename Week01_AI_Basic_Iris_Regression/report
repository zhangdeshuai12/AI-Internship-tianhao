一、项目背景
这周是AI科研实践线上实习的第一周，主要任务是先熟悉一下AI相关的基础工具，注册好GitHub和Kaggle的账号，然后动手做两个入门级的机器学习项目练练手。一个是Iris鸢尾花分类，另一个是加州房价预测，正好一个对应分类问题，一个对应回归问题。从数据探索、画图分析，到训练模型、对比效果，整个流程都跑了一遍，算是把机器学习的基本套路走通了。

二、Iris鸢尾花分类实验
2.1 数据长什么样
Iris数据集是个非常经典的入门数据集，一共150条样本，每条有4个特征：花萼长度、花萼宽度、花瓣长度、花瓣宽度，标签是3种不同的鸢尾花。数据很干净，没有缺失值，三种花每种正好50条，非常均衡。

我做了一个相关性热力图，发现花瓣长度和花瓣宽度之间的相关系数高达0.96，说明这两个特征高度相关。散点图也很直观——Setosa这一类的花瓣明显比另外两种小一圈，而且和其他两类完全没有重叠。说白了，只要你量一下花瓣，基本就能判断是哪一类花了。

2.2 模型训练和效果对比
把数据按7:3分成了训练集和测试集，因为KNN和逻辑回归对数据的尺度比较敏感，所以先做了标准化处理。选了三个模型来对比：

模型	             准确率
Logistic Regression	 0.9111
KNN (k=5)	         0.9111
Decision Tree	     0.9333
从混淆矩阵来看：

Logistic Regression：搞混了4个样本，其中1个Versicolor被认成了Virginica，3个Virginica被认成了Versicolor。

KNN：有4个Virginica被误判成Versicolor，其他都对了。

Decision Tree：有3个Versicolor被误判成Virginica，其他都对了。

总体来看，决策树这次表现最好，准确率93.33%，不过三个模型差距不大，都属于能用的水平。

2.3 哪个特征最重要
为了搞清楚"到底哪个特征对分类最有用"，我用了两种方法分析：

方法一：决策树给的特征重要性

特征	重要性
花瓣长度	0.5641
花瓣宽度	0.4226
花萼长度	0.0133
花萼宽度	0.0000

方法二：逻辑回归的系数绝对值

特征	系数绝对值
花瓣宽度	1.5973
花瓣长度	1.2106
花萼长度	0.7182
花萼宽度	0.6364
两种方法结论一致：花瓣特征最重要。决策树觉得花瓣长度略胜一筹，逻辑回归觉得花瓣宽度更有区分度，但本质上都是花瓣说了算。这和我之前在散点图里看到的完全吻合——三种花的花瓣特征区分度非常明显。

三、加州房价预测实验
3.1 房价数据长什么样
这个数据集有20640条样本，8个特征，包括收入中位数、房龄、房间数、人口、经纬度等。目标是要预测房价中位数。

我先画了房价的分布图，发现数据是右偏的——大部分房价集中在5万到30万美元之间，但也有少数特别贵的房子。再看相关性，收入中位数和房价的相关性最高，大概0.69，说明收入确实是影响房价最重要的因素。经纬度也和房价有相关性，印证了那句老话——买房就是买地段。另外，平均房间数和平均卧室数高度相关为0.85，这俩特征有点重复，可能存在多重共线性的问题。

3.2 模型训练和指标对比
我用了两个模型来做预测：普通线性回归和带正则化的岭回归。数据先做了标准化处理，两个模型在测试集上的表现如下：

模型	MSE	RMSE	MAE	R²
Linear Regression	0.5559	0.7456	0.5332	0.5758
Ridge (alpha=1.0)	0.5559	0.7456	0.5332	0.5758

3.3 结果怎么看
两个模型的R²大概都是0.576，也就是说模型能解释大约57.6%的房价变化。岭回归稍微好一点点，从0.575788提升到0.575816，说明加了正则化之后确实有点用，但提升幅度非常有限。

从"真实值vs预测值"的散点图来看，中等价位的房子为1.0~3.0区间，预测得还行，但超过4.0的高价房，预测值普遍偏低——模型对贵房子有点"估不准"。残差图也印证了这一点，房价越高，误差越大。说明线性模型在处理高价房这种"长尾"样本时有点吃力，后面可以考虑试试非线性模型，或者把房价取个对数再建模。

四、结果解读
4.1 鸢尾花分类项目结果解读
鸢尾花数据集（Iris Dataset）是一个经典的线性可分数据集，旨在通过花萼与花瓣的几何特征区分三个品种。以下是对产出图表的逐一分析。

4.1.1 类别分布图（iris_distribution.png）
该图展示了三个类别（setosa、versicolor、virginica）的样本频数。图表显示，每个类别的样本数量均为 50。这是一个完全平衡的数据集。在分类任务中，平衡数据集意味着可以直接使用"准确率（Accuracy）"作为可靠的模型评估指标，无需担心类别倾斜带来的评估失真。若模型进行随机猜测，其准确率仅为 33.3%，这为后续评估模型真实性能提供了清晰的基线。

4.1.2 特征相关性热力图（iris_correlation.png）
该矩阵展示了四个特征之间的皮尔逊相关系数。关键发现如下：
花瓣长度与花瓣宽度呈极强正相关（0.96）：这两个特征几乎包含完全相同的趋势信息，存在明显的多重共线性。在逻辑回归等线性模型中，这意味着保留两者会浪费参数自由度，实际建模时可考虑降维或仅取其一。
花瓣长度与花萼长度呈强正相关（0.87）：虽然数值较高，但这主要是由于花瓣长度本身的区分度极高所导致的连带效应。
花萼宽度与花瓣长度呈负相关（-0.43）：这在生物学上暗示了鸢尾花生长过程中的一种"资源权衡"策略。
综合来看，花瓣相关的特征（长度和宽度）与类别标签的区分度最高，是后续分类建模的核心驱动力。

4.1.3 特征散点图（iris_feature_plot.png）
该图以花瓣长度为横轴、花瓣宽度为纵轴，并按不同物种着色。可视化结果清晰地揭示了数据的分布结构：
Setosa 的完全隔离：该类别的所有样本点紧密聚集在左下方（花瓣短而窄），与另外两类之间存在巨大的空白间距。这意味着即便使用最简单的阈值判断，也能 100% 准确识别出 Setosa。
Versicolor 与 Virginica 的重叠区：后两个类别的样本在图表中部存在明显的交叉重叠区域。这直接解释了为什么任何机器学习模型都无法在该数据集上达到 100% 的准确率——因为自然界中这两类的形态特征确实存在模糊地带。模型的误判样本 100% 发生在这个重叠区域内。

4.1.4 混淆矩阵（iris_confusion_matrix.png）
基于三个模型 91%~93% 的准确率反推混淆矩阵的结构：
矩阵对角线上的数值代表正确预测的样本数，数值较大。
由于 Setosa 在特征空间中被完全隔离，其预测错误数极大概率接近 0。
所有的分类错误均集中在 Versicolor 被误判为 Virginica，以及 Virginica 被误判为 Versicolor。这一模式完美验证了散点图的结论，精准定位了模型的薄弱环节。

4.1.5 模型指标表（iris_metrics.csv）
模型	准确率
Logistic Regression	0.911
KNN (k=5)	        0.911
Decision Tree	    0.933

表格解读：
逻辑回归（91.1%）：作为线性分类器，达到了极高的准确率，证明该数据集存在清晰的线性决策边界。
KNN（91.1%）：基于距离的投票机制表现稳健，但 k=5 的默认参数在重叠区域的边界处理上较为平滑。
决策树（93.3%）：表现最优，高出前两者 2.2 个百分点。决策树通过非线性的"直角切割"策略，在 Versicolor 和 Virginica 的模糊重叠区边缘多捕捉到了几个正确样本。然而，这 2.2 个百分点的微弱提升也伴随着过拟合的风险，若更换测试集，决策树的稳定性可能不如逻辑回归。

4.2 加州房价预测项目结果解读
加州房价数据集（California Housing）是一个典型的回归任务，特征包含收入、房龄、房间数及地理位置等。线性回归与 Ridge 回归在该数据集上的表现揭示了诸多现实建模中的挑战。

4.2.1 目标变量分布图（housing_target_dist.png）
图表显示房屋中位价（单位：$100,000）呈现明显的右偏分布，即大多数房屋价格集中在低中区间（1~3，即 10 万~30 万美元），少数房屋价格较高。
尤其值得注意的是，在数值 5.0（50 万美元） 处，频次并非自然衰减，而是突然异常堆高。这是该数据集官方为处理离群点而进行的人为截断。重要隐患：这意味着模型在训练时从未见过高于 50 万美元的真实样本。若将该模型应用于实际业务预测高价豪宅（如 80 万美元），模型将铁定输出接近 40~50 万美元的数值，产生严重低估的系统性偏差。

4.2.2 特征相关性热力图（housing_correlation.png）
该矩阵展示了所有特征与目标变量之间的相关系数：
MedInc（收入中位数）与 MedHouseVal（房价）相关系数为 0.69：这是全矩阵中最强的相关性，远高于其他特征。这表明"社区收入水平决定房价"是最核心的线性规律，模型在分配权重时，MedInc 会占据绝对主导地位。
AveRooms 与 AveBedrms 相关系数为 0.85：二者高度冗余。这种多重共线性会导致回归系数失去物理意义——模型可能给"房间数"极高正权重，同时给"卧室数"极高负权重，虽然预测值不变，但系数完全扭曲。
Latitude 与 Longitude 相关系数为 -0.92：经纬度在地理上呈天然的对角线负相关。由于模型仅以原始数值输入，这两列特征与房价的相关性极弱（-0.14 和 -0.05），不仅无法捕捉"离海湾远近"等非线性地理效应，还占用了模型自由度。

4.2.3 回归模型指标表（metrics.csv）
模型	MSE	RMSE	MAE	R²
Linear Regression	0.5559	0.7456	0.5332	0.5758
Ridge (alpha=1.0)	0.5559	0.7456	0.5332	0.5758

表格解读：
R² 仅为 0.576：模型只能解释目标变量约 57.6% 的方差，剩余 42.4% 属于非线性因素或噪音。线性模型在该任务上连及格线（0.6）都未达到。
RMSE 为 0.745（即 7.45 万美元）：考虑到加州房价中位数约 20~30 万美元，平均预测误差超过 30%，在实际业务中完全不可接受。
Ridge 无任何提升：两者指标差异出现在小数点后第 5 位，说明 alpha=1.0 的正则化惩罚力度未起效，也暗示当前特征间的共线性并未严重到导致普通线性回归系数发散的地步。

4.2.4 真实值 vs 预测值散点图（regression_pred_vs_true.png）
该图展示了模型拟合的全局表现：
明显的"均值回归"现象：点云虽然沿对角线分布，但斜率明显小于 1。对于低价房（真值 < 2），模型预测普遍偏高；对于高价房（真值 > 4），模型预测普遍偏低。预测值被强烈拉向训练集的均值（约 2.0）附近。
高价区预测缺失：在预测值大于 4.0 的区域几乎没有样本点，直观证明了模型完全不具备预测极高价房屋的能力，这正是目标变量被截断的直接后遗症。

4.2.5 残差直方图与 QQ 图（residual_histogram.png 与 residual_qq.png）
正态性假设验证：直方图呈现对称的钟形分布，QQ 图的散点基本贴合理想直线（除两端极少数离群点外）。结论是残差近似满足正态分布，模型不存在系统性的估计偏差（均值接近 0）。
尾部离群值：QQ 图左下角略微偏离直线，说明数据中存在个别实际房价远低于模型预测的极端案例，它们对模型参数产生了一定拉扯，但尚在可控范围内。

4.2.6 残差 vs 预测值散点图（residual_plots.png 与 residual_scatter.png）
这是诊断线性模型是否适用的决定性图表。图中标注了残差均值约为 0.0035，标准差约为 0.7455。
检测到异方差性：观察残差的分布宽度，当预测值在 1.0~2.0 区间时，残差波动极小（紧密围绕零线）；但随着预测值增大至 3.5 以上，残差的波动幅度急剧扩大，呈现明显的"喇叭口"形状。
业务与模型含义：模型在预测中低房价区域时稳健且准确，但一旦特征组合指向高价区域，模型便束手无策，预测结果飘忽不定。这铁证了线性模型的函数结构无法承载高价区的复杂非线性影响因素。

4.3 两个项目的交叉对比总结
将鸢尾花与房价项目的产出放在一起对比，能得出本周最关键的两个认知：
Iris 是验证流程的"友好玩具"：数据完美线性可分，特征纯净，即使是基础算法也能轻易突破 90% 准确率。它适用于跑通完整的机器学习开发流程，但无法体现模型调参与特征工程的真正价值。
房价是逼近现实的"严酷警钟"：特征冗余、目标截断、高度非线性、严重异方差——这些问题在真实业务中极为常见。R²=0.576 仅是一个虚假的安全感，警示我们优秀的指标数值不等于业务可用。若要真正解决房价预测问题，下一步必须抛弃线性模型，进行深度特征衍生（如构造距离海岸线距离、人均房间数），并引入随机森林或 XGBoost 等非线性集成算法。

五、工具使用和遇到的问题
5.1 环境配置
这周搞定了基础环境：注册了GitHub和Kaggle账号，装了Python和必要的库，如scikit-learn、pandas、matplotlib、seaborn。平时用VS Code写代码，在终端里运行脚本。

5.2 遇到的问题
路径报错：第一次运行代码的时候，程序想往results/文件夹里存图片，但这个文件夹不存在，直接报错了。解决办法是在代码开头加上一行os.makedirs('results', exist_ok=True)，让它自动创建文件夹。后面根据任务书要求，图片要放在results/figures/里，同样用这个方法处理。

中文显示问题：matplotlib默认字体不支持中文，导致图表的标题和标签里的中文都显示成方框。虽然不影响图片保存和模型训练，但看着不太美观。后面可以尝试装个中文字体或者设置plt.rcParams['font.sans-serif'] = ['SimHei']来解决。

标准化的重要性：一开始做Iris分类的时候忘了标准化，KNN的效果不太好。后来意识到KNN是靠距离算相似度的，不同特征的尺度不一样会影响结果，加了标准化之后就好了。

5.3 这周学到了什么
通过这两个小项目，我算是把机器学习的基本流程完整走了一遍：拿到数据先看一看、画个图探索一下 → 做必要的预处理（比如标准化）→ 选个基线模型跑一跑 → 多试几个模型对比效果 → 分析结果、看看哪里还能改进。Iris这个项目让我搞清楚了分类问题怎么看准确率和混淆矩阵，房价项目让我学会了回归问题怎么用MSE、R²来评估模型，也理解了正则化到底有什么用。

